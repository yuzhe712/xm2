# IntelliTicket — 企业级智能工单自动化处理平台项目开发规范

## 0. 开发行为准则

**权衡取舍：** 偏向谨慎而非速度。对琐碎任务自行斟酌。

### 0.1 编码前先思考

**不主观臆断。不掩盖疑惑。把权衡取舍摆到台面上。**

实现之前：
- 明确陈述假设。不确定时主动提问。
- 存在多种解释时列出选项，不默默自行决定。
- 有更简单的方案就提出来，有正当理由时提出反对意见。
- 任何不清楚的地方立即停下来，指明困惑点，提问。

### 0.2 工程价值与主动设计判断

**本项目目标是开发能在企业工单场景中真实降本增效的软件，而不是为了展示概念的玩具 demo。**

当用户通过 skill、slash command 或自然语言请求生成 Agent、Tool、Orchestrator、API 或架构组件时，Claude Code 不能机械套模板。必须先判断：

- 该组件是否符合当前项目阶段和业务目标；
- 设计是否能提升真实处理效率、可维护性、可观测性或证据可信度；
- 是否存在与真实软件开发不一致、不合理、过度设计或安全边界不清的问题；
- 是否会把 mock/demo 能力伪装成真实生产能力；
- 是否会为了“看起来智能”牺牲确定性、可验证性和证据闭合。

如果发现用户给出的模板、slash command 默认方案或前序方案不合理，必须主动指出问题，给出更合适的工程方案，并等待用户审核后再实现。

判断优先级：

1. 业务价值和降本增效；
2. 正确性、证据闭合和安全边界；
3. 可维护性、可测试性和可观测性；
4. 技术展示效果。

允许修改原方案。方案不是命令本身，方案必须服务于项目目标。

### 0.3 简单优先

**用最少代码解决问题。不做预测性开发。**

- 不添加要求之外的功能。
- 不为一次性代码做抽象。
- 不提供未被要求的“灵活性”或“可配置性”。
- 不可能发生的场景不写错误处理。
- 写了 200 行但其实 50 行能搞定，就重写。

问自己：“高级工程师会认为这太复杂了吗？”如果是，简化。

### 0.4 外科手术般的精准修改

**只改必须改的地方。只清理自己制造的烂摊子。**

编辑现有代码时：
- 不“改进”相邻的代码、注释或格式。
- 不重构没有出问题的东西。
- 保持现有的代码风格，即使个人倾向于不同做法。
- 注意到不相关的死代码可以提出来，但不删除。

修改产生孤立代码时：
- 删除因为本次修改而不再使用的 import、变量或函数。
- 除非明确要求，否则不删除原先就存在的死代码。

检验标准：每一行被修改的代码都应直接追溯到用户请求。

### 0.5 目标驱动执行

**定义成功标准。循环直到验证通过。**

将任务转化为可验证的目标：
- “添加验证” → “为无效输入编写测试，然后让它们通过”。
- “修复 bug” → “编写能复现该 bug 的测试，然后让它通过”。
- “重构 X” → “确保重构前后测试都能通过”。

多步任务先陈述简要计划：

```text
1. [步骤] → 验证: [检查项]
2. [步骤] → 验证: [检查项]
3. [步骤] → 验证: [检查项]
```

明确的成功标准能独立循环工作。模糊标准（“让它跑起来”）需要不断确认。

---

## 1. 项目目标

构建 **IntelliTicket：面向单一公司内部使用的智能工单自动化处理平台**。

一句话定位：面向一个公司内部 IT 运维 / 客服 / 支持场景的多 Agent 工单处理系统，能够自动理解工单、分类定级、补全上下文、初步诊断、推荐处理方案，并生成可追溯的处理报告。当前产品选择单公司单实例，不做 SaaS 多租户模型。

本项目不是普通 ChatBot，也不是传统后台管理 Web。第一版推荐形态是：

```text
Electron / React / TypeScript 桌面 App
        ↓
FastAPI 后端服务
        ↓
多 Agent 工单处理引擎
        ↓
本地模拟运维数据集 / SQLite / JSON 知识库
```

核心评价标准是：

1. 工单理解、分类、定级和分派有明确业务规则或证据支撑；
2. 上下文补全、诊断和建议可追溯、可复核；
3. 证据不足、冲突或过期时明确保留意见，不编造结论；
4. 多 Agent 协作确有业务必要，而不是为了展示技术堆栈；
5. 桌面 App 能清楚展示工单、报告、Agent 执行链路和证据。

## 2. 第一版 MVP 范围

第一阶段只做一个最小但完整的场景：**运维告警工单自动处理**。

MVP 输入示例：

```text
线上支付服务出现超时告警，订单量从正常1000/min降到300/min
```

MVP 必须输出：

1. 工单分类；
2. 优先级；
3. 影响服务；
4. 查询到的上下文；
5. 候选根因；
6. 处理建议；
7. Agent 执行链路；
8. 最终处理报告。

第一版闭环：

```text
用户输入工单
  ↓
后端生成 ticket_id
  ↓
Intake Agent 分类定级
  ↓
Context Agent 从本地 JSON / SQLite 查询上下文
  ↓
Diagnosis Agent 生成诊断与证据
  ↓
Routing Agent 生成分派与处理建议
  ↓
Report Agent 生成最终报告
  ↓
桌面 App 展示处理链路
```

第一版明确不要做：登录注册、多租户、租户后台、跨租户隔离、真实 Jira/Prometheus/Grafana/飞书接入、Redis、Celery、Kubernetes、复杂权限系统、多数据库、真正企业级部署。一个公司使用一套独立部署；如需给另一家公司使用，应部署另一套实例，而不是在当前 MVP 内引入 `tenant_id`。

## 3. 技术栈基线

| 层 | 技术选择 |
|---|---|
| 桌面 App | Electron、React、Vite、TypeScript |
| 后端 | Python 3.11+、FastAPI、asyncio、Pydantic v2 |
| 数据 | 第一版优先 JSON 文件 + 内存状态；SQLite 可作为后续持久化 |
| Agent 编排 | 单进程多 Agent 编排；需要动态规划时再考虑 LangGraph |
| Agent 通信 | 内部类型化任务信封；只有验证正式规范/SDK 后才能声明 A2A 兼容 |
| 工具协议 | 只有真实外部工具接入需要 MCP；领域服务与 MCP 传输层分离 |
| 测试 | pytest；前端按项目实际引入 Vitest/Playwright |
| 部署 | 单公司单实例；本地开发优先，内网部署需配置后端地址和 CORS；Docker Compose 只声明实际构建和验证过的服务 |

模型提供商、模型 ID、超时、重试、并发、采样参数必须配置化。不要在 Agent 或业务代码中写死。

## 4. 代码规范

- 文件、变量、函数和类使用英文；业务注释、日志与用户消息使用中文。
- Python 文件使用 `from __future__ import annotations`，公开函数必须有类型注解。
- 使用 `logging.getLogger(__name__)`，禁止用 `print()` 充当生产日志。
- 导入顺序：`__future__`、标准库、第三方库、本地模块。
- 路由只处理协议与验证，业务逻辑进入 service；数据访问进入 repository 或明确的数据访问层。
- 不创建只返回 `{}`、`[]`、固定成功消息的占位 API。
- 不保留会进入正常运行路径的 `TODO`、`pass`、假实现或硬编码演示结论。
- 不声称“生产可用”“准确率高”“已部署”或“测试通过”，除非本次会话确实观察到对应证据。

## 5. 工单证据与溯源契约

每条观测、上下文、诊断或建议所依赖的证据至少应记录：

- `evidence_id`：证据标识；
- `source_type`：`ticket_input`、`service_catalog`、`metric_snapshot`、`deployment_record`、`incident_history`、`sop_document` 等；
- `source_id` / `source_name`：数据源标识和名称；
- `observed_at` 或 `retrieved_at`：观测或获取时间；
- `service`：关联服务；
- `metric_name`、`value`、`unit`：指标名、数值和单位（如适用）；
- `quality`：数据质量、时效性或可用状态；
- `data_mode`：`demo`、`mock` 或 `real`；
- `confidence`：仅在有明确计算或校准方法时提供。

输出必须区分：

- **事实**：输入工单或数据源直接提供；
- **推导**：由规则、模型或计算得出；
- **假设**：因输入不足而采用的明确前提；
- **未知**：当前无法获得的信息；
- **建议**：基于事实、推导和 SOP 的处理动作。

LLM 生成的来源、工具名、参数、引用、置信度、优先级和路由结果都不可信，必须经过 schema、allowlist 和业务规则校验。

## 6. 模拟数据与真实集成边界

第一版可以使用本地可控模拟运维数据集，例如：

```text
mock_data/
  services.json
  incidents.json
  deploy_records.json
  metrics.json
  sop_docs.json
```

要求：

- 模拟数据必须显式标记 `data_mode: "mock"` 或 `data_mode: "demo"`。
- 前端必须显示“模拟数据”或“演示数据”标识。
- 正常运行路径不得因外部 API 失败而静默返回看似真实的模拟值。
- 预留 Prometheus、Grafana、Jira、飞书工单等系统适配点时，只能写清楚“未实现”或“扩展点”，不能伪装成已接入。
- 测试 fixture、示例数据和假模型输出不得泄漏到真实集成响应。

## 7. 分级失败策略

禁止对所有错误统一 fail-open。

| 类型 | 处理方式 |
|---|---|
| 可选上下文失败 | 可返回明确标记的部分结果，不影响已有可靠证据 |
| 诊断必需证据缺失或过期 | 对受影响根因或建议 abstain（不作结论），提示需要补充的数据 |
| 输入、服务名、指标或单位非法 | 拒绝请求并返回可操作的结构化错误 |
| 鉴权、授权失败（后续如实现） | fail-closed；当前 MVP 不实现租户隔离 |
| 溯源丢失、数据模式不明 | fail-closed，不生成看似确定的诊断建议 |
| 瞬时、幂等的外部调用失败 | 有界重试和指数退避；禁止无限重试 |
| 单个 Agent 失败 | 由编排策略判断是否部分完成、降级或终止，不得污染其他 Agent 证据 |

错误需有稳定的 `code`、可读 `message`、来源状态和可观测日志。

## 8. Agent、内部任务信封与 MCP 约束

### Agent

第一版建议的 Agent：

| Agent | 职责 |
|---|---|
| `ticket_intake_agent` | 理解工单输入，提取服务、症状、指标、类型和优先级 |
| `context_retrieval_agent` | 查询部署记录、指标快照、依赖关系、历史工单和 SOP |
| `diagnosis_agent` | 生成候选根因、证据链、置信度和不确定性 |
| `routing_agent` | 推荐处理团队、行动项、升级策略和 SOP 引用 |
| `report_agent` | 汇总最终报告，保留证据、链路和未确认事项 |

约束：

- 能用确定性 service/workflow 完成的任务，不创建 Agent。
- Agent 必须有类型化输入、输出、错误、证据与溯源契约。
- 限制最大迭代数、工具调用次数、执行时间、重试次数和并发数。
- 不保存或展示模型的私有 chain-of-thought；只记录可审计的动作、工具调用、观测、简短决策理由和结论依据。
- 单个 Agent 失败不得污染其他 Agent 的证据；是否继续由失败等级决定。

### 内部任务信封 / A2A 命名

可以先设计内部消息格式：

```json
{
  "from": "ticket_intake_agent",
  "to": "diagnosis_agent",
  "ticket_id": "TCK-001",
  "message_type": "analysis_result",
  "payload": {}
}
```

但自定义 `TaskRequest` / `TaskResult` 或上述消息格式只是内部任务信封，不得称为正式 A2A 协议实现。只有使用固定版本的正式 A2A 规范/SDK，并通过协议级契约测试后，才能声明 A2A 兼容。

跨 Agent 消息必须有 `ticket_id`、`run_id`、发送方、接收方、状态、时间戳、证据引用和幂等标识。

### MCP

- 只有接入真实外部工具或系统时才引入 MCP。
- 使用当前官方 MCP SDK 和正式支持的传输，不手写 stdin/stdout JSON-RPC 冒充 MCP。
- 工单领域逻辑放在独立 service，MCP server/client 只负责协议适配。
- MCP 工具必须有输入/输出 schema、鉴权边界、超时、限流、错误映射和溯源字段。
- 传输失败不能自动切换到语义不同的数据源。

## 9. 编排状态规范

无论使用普通 Python workflow 还是 LangGraph，都必须遵守：

- State 只包含可序列化数据；禁止放 SQLAlchemy Session、HTTP 客户端、锁或 service 实例。
- 并行节点输出使用不可变或追加式记录，例如 `list[AgentRun]`，并定义显式 reducer。
- 禁止并行节点同时覆盖共享 `dict`。
- 路由结果必须经过已知节点 allowlist，非法路由进入受控错误或安全终止节点。
- 图或 workflow 必须有确定终止条件和步骤上限。
- Synthesis / Report 必须保留 provenance、展示冲突；证据不足时 abstain，不能制造共识。
- 使用 checkpointer 时必须定义 thread/session ID、数据保留策略和清理机制。

## 10. API、桌面 App 与安全边界

在所有系统边界进行验证：HTTP、SSE/WebSocket、MCP、内部任务信封、数据库、外部 API 和 LLM 结构化输出。

重点校验：

- ticket_id、run_id、service_id、incident_id、sop_id；
- 日期、时间戳和时区；
- 服务名、团队名、指标名、单位、数值范围和空值；
- 分页、过滤、上传类型与大小；
- 外部来源时效性；
- SSE/WebSocket 的认证、断连清理、心跳、背压和事件 schema（如实现）；
- 桌面 App 到本地后端的地址、端口、错误提示和启动状态。

密钥只从环境变量或安全存储读取，禁止写入源码、提示词、日志和响应。

桌面 App 第一版建议三栏布局：

```text
左侧：工单列表 / 示例工单
中间：工单输入与最终报告
右侧：Agent 执行链路 / 推理步骤摘要 / 证据
```

前端不得展示私有 chain-of-thought，只展示可审计执行日志、工具调用摘要、证据和结论理由。

## 11. 开发工作流

1. 先读现有代码和测试，确认可以复用的实现。
2. 多文件或多阶段改动先给出计划和明确的文件清单。
3. 每次只完成一个可运行的垂直切片，不批量制造空壳模块。
4. 开发者审查 diff，确认业务与架构决策；Claude Code 负责实现和修复。
5. 运行受影响的静态检查、单元测试和端到端验证。
6. 如实汇报执行过、通过、失败和跳过的验证项。
7. 完成后再进入下一个里程碑。

并行开发时，独立领域模块可以并行；`registry`、公共 schema、路由入口、编排器和数据库迁移等共享文件必须统一协调。

## 12. 完成标准

一个功能只有满足以下适用项才算完成：

- 格式化、lint 和类型检查通过；
- 领域 service 有单元测试；
- API 有 schema、边界和错误测试；
- Agent/编排器有超时、失败、非法路由、冲突和证据不足测试；
- mock 数据路径明确标记 `data_mode`，前端可见；
- 工单 eval 覆盖缺失、过期、冲突、模拟数据、异常单位、服务名识别失败和优先级边界；
- 运行真实受影响流程，观察到预期行为；
- 文档明确哪些能力使用模拟数据、哪些仍是扩展点或未实现。

## 13. 项目本地 Skills

| Skill | 使用场景 |
|---|---|
| `/bootstrap-project` | 从最小可运行垂直切片开始初始化 IntelliTicket |
| `/create-agent` | 判断并创建确有必要的工单处理 Agent |
| `/create-mcp-tool` | 使用官方 SDK 创建外部系统 MCP 工具或服务 |
| `/create-orchestrator` | 创建或修改工单多 Agent 编排 |
| `/create-api` | 设计并实现 REST、SSE 或 WebSocket API |
| `/review-architecture` | 基于实际代码审查架构、协议和工单证据质量 |
| `/design-decision` | 在重大架构和技术选型前做结构化决策 |
